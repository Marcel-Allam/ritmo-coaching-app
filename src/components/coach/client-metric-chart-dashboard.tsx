'use client';

// Dashboard entrypoint: uses per-card chart configuration only.
export { ClientMetricChartDashboardCardConfig as ClientMetricChartDashboard } from './client-metric-chart-dashboard-card-config';
